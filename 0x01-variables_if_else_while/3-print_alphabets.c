#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char k;

	for (k = 'a'; k <= 'z'; c++)
		putchar(k);
	for (k = 'A'; k <= 'Z'; c++)
		putchar(k);
	putchar('\n');
	return (0);
}
