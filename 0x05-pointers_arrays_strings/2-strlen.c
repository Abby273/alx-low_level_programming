#include "main.h"
#include <stdio.h>

/**
 * _strlen - length of a string
 * @s: input char
 * Return: length of a string
 */

int _strlen(char *s)
{
	int a;
	a = *s;

	s = "My line";
	printf("%d\n", a);
	return (0);
}
